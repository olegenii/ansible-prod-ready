- hosts: webserver
  become: yes
  
  vars:
    # List of virtualhosts
    virtualhosts:
      1:
        name: "{{ tf_hostname }}"
  
  roles:
    - role: nginx
      tags: nginx
    - role: letsencrypt
      tags: cert