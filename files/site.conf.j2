server {
  listen {{ port_num }};

  root /var/www/{{ item.value.name }}/html;
  index index.html index.htm;

  server_name {{ item.value.name }};

  location / {
    default_type "text/html";
    try_files $uri.html $uri $uri/ =404;
  }
}