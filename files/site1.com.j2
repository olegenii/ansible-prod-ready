server {
  listen {{ port }};

  root /var/www/{{ virtualhosts[1].name }}/html;
  index index.html index.htm;

  server_name {{ virtualhosts[1].name }};
  
  location / {
   default_type "text/html";
   try_files $uri.html $uri $uri/ =404;
  }
}